import assert from 'node:assert/strict'; import { readFile } from 'node:fs/promises'; import test from 'node:test';
const text=p=>readFile(new URL(`../${p}`,import.meta.url),'utf8');
test('payslip mobile is a final read-only server surface',async()=>{const api=await text('mobile/src/features/payslips/payslip-api.ts');const sql=await text('supabase/migrations/20260925080601_goal_374_employee_final_payslip_read.sql');const detail=await text('mobile/app/payslips/[month].tsx');assert.match(api,/get_my_final_payslip_list/);assert.match(api,/get_my_final_payslip/);assert.doesNotMatch(api,/insert|update|delete/i);assert.match(sql,/payroll_ledger_confirmed/);assert.match(sql,/record_role = 'as_paid'/);assert.doesNotMatch(sql,/payroll\.manage/i);assert.match(detail,/계산하거나 수정할 수 없습니다/);});
